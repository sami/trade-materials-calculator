/**
 * Placeholder engine export. It exists only so the engine import boundary and
 * the test runner can be exercised before any real calculation is written.
 */
export function ping(): "engine" {
  return "engine";
}
